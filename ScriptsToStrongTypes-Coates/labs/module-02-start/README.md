# Module 02 Starter — Your First Cmdlet

This is the completed state of Module 01. The project is scaffolded and the
PowerShell SDK reference is in place. `GetGreetingCmdlet.cs` is a stub —
your job in Module 02 is to build it out.

## What's here

- `src/PsStrongTypes/PsStrongTypes.csproj` — Class Library targeting netstandard2.0
  with `Microsoft.PowerShell.SDK` referenced
- `src/PsStrongTypes/GetGreetingCmdlet.cs` — empty PSCmdlet subclass stub

## Verify the starting point

```pwsh
dotnet build src/PsStrongTypes
```

Build should succeed with 0 errors before you begin.
